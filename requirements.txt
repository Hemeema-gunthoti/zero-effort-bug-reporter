# Testing
pytest>=7.4.0
selenium>=4.15.0
pytest-html>=4.0.0

# Web app
Flask>=3.0.0
gunicorn>=21.0.0
# AI — Groq (OpenAI-compatible)
groq>=0.9.0

# MCP
mcp>=1.0.0

# HTTP
httpx>=0.27.0

# Notifications
# (Slack removed — email only via built-in smtplib)

# Redis (deduplication + caching)
redis>=5.0.0

# Utilities
python-dotenv>=1.0.0
Pillow>=10.0.0
